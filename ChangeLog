ChangeLog

1.4

- Display milliseconds to the third decimal point
- Fix indexing to match Blu-ray spec for playlist numbers

bluray_copy:

- can now stream to stdout
- Print errors to stderr
- Increase default buffer sizes
- Increased buffer size for Blu-ray optical drives
- Don't require an output filename, use defaults if none given
- Optional quiet output to remove printing copy progress

bluray_info:

- Display main title even if one track is listed
- Fix displaying video and audio streams

1.3

- Add autotools

1.2.0
- initialize structs properly
- bluray_copy quits on disk space full

1.1.0
- add bluray_copy

1.0.0
- Initial release
